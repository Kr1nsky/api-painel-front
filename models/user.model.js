class User {
    constructor(username, password) {
        this.username = username;
        this.password = password;
    }
    // Outros métodos e propriedades do modelo de usuário podem ser adicionados aqui
}

module.exports = User;